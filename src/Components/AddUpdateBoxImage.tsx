import React, {useState} from 'react';
import {CategoryDto} from "../types";

interface AddUpdateBoxImageProps {
}

const AddUpdateBoxImage: React.FC<AddUpdateBoxImageProps> = () => {

    return(
        <></>
    );
};

export default AddUpdateBoxImage;